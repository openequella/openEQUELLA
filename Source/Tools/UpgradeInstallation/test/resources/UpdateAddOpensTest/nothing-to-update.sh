# As this already has the JAVA_ADDOPENS variable, there upgrader should make no changes.
export JAVA_ADDOPENS=value is irrelevant here

# Regardless that there is nothing else in this file, the above variable declaration should be
# enough to stop any changes.
